
          #!/bin/bash
          # This script is used to setup the Rippling CLI in the Codespace
          # It is run as part of the postCreateCommand in the devcontainer.json file
          # It is used to install the Rippling CLI and configure it for the current user
          # It is also used to install the required dependencies for the app
          pip install requirements.txt
          